#ifndef _YAHM_INTERNALS_
#define _YAHM_INTERNALS_

////////////////////////////////////////////////////////////////////////////////
#define REINTERPRET_CAST(type, exp)  ((type)(exp))
////////////////////////////////////////////////////////////////////////////////
static inline UInt32 SwapPtr32(void *p){
	UInt32 x = REINTERPRET_CAST(UInt32,p);
	return ByteSwap32(x);
}
////////////////////////////////////////////////////////////////////////////////
static inline void * SwapPtr(void *p){
	UInt32 x = REINTERPRET_CAST(UInt32,p);
	return REINTERPRET_CAST(void *, (ByteSwap32(x)));
}
////////////////////////////////////////////////////////////////////////////////
static inline UInt32 SafeMemPtrSize(void *p){
	return (p == NULL) ? 0 : MemPtrSize(p);
}
////////////////////////////////////////////////////////////////////////////////
static inline void *SafeMemHandleLock(MemHandle h){
	return (h == 0) ? 0 : MemHandleLock(h);
}
////////////////////////////////////////////////////////////////////////////////
static inline void SafeMemHandleUnlock(MemHandle h){
	if  (h != 0){
		 MemHandleUnlock(h);
	 }
}
#endif
